# Design: Compare baseline

Issue #28. Tokens from `docs/UI_STYLEGUIDE.md`.

## Intent
Hard compare only when locks match. Incomparable is a first-class status, not a red budget.

## Layout
- `/compare`: baseline/candidate ids, Compare, result table (absolute, delta, noise), Confirm baseline
- No single score

## Out
Remote hardware farm, auto-baseline after green.
