# Feature: VisoDev Import Schema

<!-- auto-generated by /implement on 2026-08-13 — issue #35 -->

## Intent
FR-026: Potato Boost produces artifacts independently. A documented import schema (IDs identical to Run/Evidence) is sufficient; no VisoDev code in the core.

## Preconditions
- `packages/schemas` exists (#4)
- Golden artifact exists (#9)

## Happy Path
- [x] Import schema is versioned and shares runId/evidenceId/findingId with CON-001
- [x] Documentation clearly says VisoDev is optional
- [x] No visodev npm dependency in core
- [x] Touched files: zero type escape hatches (typed-strict / Boy Scout)

## Edge Cases
- [x] Export error does not change the Potato run

## Regression
- [x] Existing packages and `pnpm checks` stay green
- [x] Scope: `docs/artifact-schema.md`, `packages/schemas/**`

## Assumptions
- Run artifact schema is canonical
- Import schema is a subset with identical IDs

## Security Coverage
- No automatic upload
- No secrets in schema

## Screenshots
Not applicable.

## Implementation Notes
- `docs/artifact-schema.md` documents the import contract.
- `packages/schemas/visodev-export.schema.json` is the JSON Schema subset.
- Golden artifact roundtrip test validates the contract.
- No VisoDev dependency; no network calls.
- All 59 tests pass; `pnpm checks` is green.
