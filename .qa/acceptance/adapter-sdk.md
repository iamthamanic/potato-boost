# Feature: Adapter SDK

<!-- auto-generated by /implement on 2026-08-13 — issue #32 -->

## Intent
Detect, Doctor, Launch, Scenario Driver, Collector, Static Provider, Source Mapper are separate capabilities (FR-024, CON-005). The core loads adapters via manifest + allowlist + version lock.

## Preconditions
- `packages/schemas` exists (#4)
- Golden artifact exists (#9)

## Happy Path
- [x] Manifest requires id, version, capabilities, detectors
- [x] Contract harness checks that optional capabilities may be missing, but required schemas apply
- [x] Core files do not need changes for a fake adapter (test documents the boundary)
- [x] Touched files: zero type escape hatches (typed-strict / Boy Scout)

## Edge Cases
- [x] Unknown capability → ignore or reject documented
- [x] Schema major mismatch → reject

## Regression
- [x] Existing packages and `pnpm checks` stay green
- [x] Scope: `packages/adapter-sdk/**`

## Assumptions
- Schema major version is 1
- Fake adapter is sufficient for contract tests
- Allowlist is local and static for MVP

## Security Coverage
- B-07: least privilege — adapters only get declared capabilities
- No eval/exec of adapter code
- Allowlist is local

## Screenshots
Not applicable.

## Implementation Notes
- `packages/adapter-sdk/` holds manifest schema, capability enum, and contract harness.
- `runContractHarness` validates manifest and checks schema major compatibility.
- Unknown capabilities are rejected by Zod enum.
- Fake adapter tests document the boundary; no core changes needed.
- All 57 tests pass; `pnpm checks` is green.
