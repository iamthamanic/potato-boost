# Feature: Report Export

<!-- auto-generated by /implement on 2026-08-13 — issue #29 -->

## Intent
FR-022, FR-026: the same file for human and machine. HTML is static (CI artifact), JSON is the schema. Export errors must not change the local measurement result.

## Preconditions
- `packages/schemas` and `packages/artifact-store` exist (#4, #5)
- Golden artifact exists (#9)

## Happy Path
- [x] JSON export validates against the run-artifact schema
- [x] HTML export references the same runId/ruleId/findingId as JSON
- [x] Export is purely local, no network
- [x] Touched files: zero type escape hatches (typed-strict / Boy Scout)

## Edge Cases
- [x] HTML is readable without a server
- [x] Export failure does not alter the local measurement result

## Regression
- [x] Existing packages and `pnpm checks` stay green
- [x] Scope: `packages/report/**`

## Assumptions
- Golden artifact is the canonical test input
- HTML is static

## Security Coverage
- P-02: no stack traces or internal paths in export
- No network calls
- No secrets in report

## Screenshots
Not applicable.

## Implementation Notes
- `packages/report/` exports validated JSON + static HTML.
- `exportReport` validates against `runArtifactSchema` before writing.
- HTML template references same runId/ruleId/findingId as JSON.
- No network calls; no secrets.
- All 51 tests pass; `pnpm checks` is green.
