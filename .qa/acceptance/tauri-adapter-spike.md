# Feature: tauri adapter spike

<!-- auto-generated by /implement on 2026-08-15 — issue #46 -->

## Intent
A Tauri fixture is detected from `src-tauri/tauri.conf.*`. Frontend/webview and native modes are separate labels. Unimplemented native collectors are `unsupported`, never silent zeros or hardware-validated claims.

## Preconditions
- Adapter SDK (#32)
- Generic unknown mode (#34)

## Happy Path
- [ ] `src-tauri/tauri.conf.*` is detected with evidence
- [ ] Frontend and native modes are separate labels
- [ ] Unimplemented native collectors are unsupported, not silent 0
- [ ] Touched files: zero type escape hatches

## Edge Cases
- [ ] Webview-only measurement is not labeled hardware-validated native
- [ ] Detect is read-only

## Regression
- [ ] Godot and web-threejs detect unchanged
- [ ] Out: full Rust IPC instrumentation
- [ ] Out: adapter-web internals

## Assumptions
- Kind `tauri` is adapter-owned; core `CandidateKind` is unchanged
- UI copy is English
- Dashboard shows the kind label only

## Security Coverage
- Detect is read-only
- F-03: no tokens in localStorage
- Out of scope: F-01, B-01, Rust IPC

## Screenshots
| Step | Filename |
|------|----------|
| — | n/a (CLI detect/doctor) |

## Implementation Notes
- New `@potato-boost/adapter-tauri` detects `src-tauri/tauri.conf.*` with evidence.
- Doctor checks `tauri-frontend` and `tauri-native` are separate; native is unsupported and not required.
- CLI/local-api merge Tauri candidates without changing core schemas.
- Dashboard `kindLabel("tauri")` is "Tauri".

## Composition Gate
- Verdict: CLEAR
- HEAD_SHA: WORKTREE
- Event: detect tauri.conf → doctor labels frontend vs native; native collector stays unsupported.
- N-actors: one repo → one tauri candidate + two mode checks.
- Invalid: no silent 0 samples; webview is not labeled native.
- Two consumers: CLI detect/doctor and local-api detect/doctor share the adapter.
